spring:
  datasource:
    # 'localhost' refers to your actual Linux machine
    url: jdbc:mysql://localhost:3306/finance_tracker_db?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: Root@1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true